{
  "name": "tv-info-panel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lightweight office TV dashboard with weather, stocks, calendar, and notes.",
  "scripts": {
    "dev": "node --watch server/server.js",
    "start": "node server/server.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:update-snapshots": "playwright test --update-snapshots"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "node-ical": "^0.26.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
